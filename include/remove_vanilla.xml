<?xml version="1.0" encoding="UTF-8"?>
<manifest>
    <!-- Projects which need to be removed -->

    <!-- hammerhead-kernel project size grew to over 8GB and caused repo sync issues for users -->
    <remove-project name="a/aosp/device/moto/shamu-kernel" />
    <remove-project name="a/aosp/platform/external/libdrm" />

    <!-- Remove duplicate projects inherited from fugu that are also in bsp -->
    <remove-project name="a/aosp/platform/hardware/intel/common/libmix" />
    <remove-project name="a/aosp/platform/hardware/bsp/intel" />

    <!-- Remove unwanted repos from vanilla manifest -->
    <remove-project name="a/bsp/hardware/intel/crm" />
    <remove-project name="a/bsp/hardware/intel/PRIVATE/IFX-modem"/>
    <remove-project name="a/bsp/hardware/intel/PRIVATE/Modem_Updater"/>
    <remove-project name="a/bsp/hardware/intel/PRIVATE/NVM_Manager"/>
    <remove-project name="a/bsp/hardware/intel/rapid_ril"/>
    <remove-project name="a/bsp/vendor/intel/featsetclass_tel"/>
    <remove-project name="a/bsp/vendor/intel/featsetres_tel"/>
    <remove-project name="a/bsp/vendor/intel/PRIVATE/imsservices"/>
    <remove-project name="a/bsp/vendor/intel/PRIVATE/imsstack"/>

</manifest>
